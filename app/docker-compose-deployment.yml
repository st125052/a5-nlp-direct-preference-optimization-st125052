services:
  flask-app:
    image: st125052/a5-direct-preference-optimization:latest-deployment
    ports:
      - "5000:5000"
    volumes:
      - /mnt/docker-data/my_service_data:/app/data
    restart: always